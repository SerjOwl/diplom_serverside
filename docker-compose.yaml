networks:
  dev:

  services:
    nginx:
      image: nginx:stable-alpine
      ports:
        - "80:80"
      volumes:
        - './nginx.conf:/etc/nginx/nginx.conf'
      depends_on:
        - llm
        - adminpanel
        - apidb
      networks:
        - dev

    llm:
      build:
        context: ./llmapi
      networks:
        - dev

    adminpanel:
      build:
        context: ./sky-admin
      networks:
        - dev
    
    apidb:
      build:
        context: ./SkyServer
      networks:
        - dev